<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= cl_image_path @list.photo.key %>');">
  <div class="container">
    <h1><%= @list.name %></h1>
  </div>
</div>
<br>
<div class="container">
  <%= link_to "Add a bookmark", new_list_bookmark_path(@list), class:"btn btn-light" %>
  <br>

  <br>
  <%= link_to "Back to lists", lists_path, class:"btn btn-light" %>
  <br>
</div>
